# Áreas pendentes

Bounded Contexts que permanecem `IMATURA` ou `PARCIALMENTE_MADURA` ao final da fase (seção 4.4 do
plano) — resultado válido, não falha. Cada entrada registra `bounded_context_id`, `maturidade`,
`blocking_ids[]`, `blocking_reason` e `required_resolution` (seção 6, "Contratos adicionais").

Vazio no momento — preenchido por `AC-029`, depois de `AD-06` classificar a maturidade de cada
`CTX-NNN`.
