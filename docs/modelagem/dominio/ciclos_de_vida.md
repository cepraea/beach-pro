# Ciclos de vida — domínio

Ciclos de vida promovidos ao Modelo Canônico (`schemas/schema_elemento_modelo.json`,
`tipo=CICLO_DE_VIDA`, `estagio=DOMINIO`) — sempre `estado_epistemologico=VALIDADO`, com
`promoted_from`/`promoted_by` preenchidos.

Nenhum ciclo de vida promovido ainda.
