# Agregados — domínio

Agregados promovidos ao Modelo Canônico (`schemas/schema_elemento_modelo.json`, `tipo=AGREGADO`,
`estagio=DOMINIO`) — sempre `estado_epistemologico=VALIDADO`, com `promoted_from`/`promoted_by`
preenchidos.

Nenhum agregado promovido ainda.
