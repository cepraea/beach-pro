# Identidades definitivas — domínio

Identidades promovidas ao Modelo Canônico (`schemas/schema_elemento_modelo.json`,
`tipo=IDENTIDADE`, `estagio=DOMINIO`) — sempre `estado_epistemologico=VALIDADO`, com
`promoted_from`/`promoted_by` preenchidos (rota A: `SEM-NNN` a partir de um candidato; rota B:
`PRE-SEED`).

Nenhuma identidade promovida ainda.
